{
  "name": "@nlp-slack/extract-images",
  "version": "0.0.0",
  "description": "",
  "keywords": [],
  "author": "Jeremy Green <hello@jeremy.green>",
  "license": "ISC",
  "main": "index.js",
  "scripts": {
    "build": "mkdir -p ../../tmp && zip -r ../../tmp/extract-images.zip .",
    "update": "aws lambda update-function-code --function extractImages --zip-file fileb://../../tmp/extract-images.zip",
    "publish": "npm run lint && npm run build && npm run update",
    "lint": "npx eslint index.js",
    "test": "echo \"Error: run tests from root\" && exit 1"
  }
}
