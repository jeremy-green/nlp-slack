{
  "name": "@nlp-slack/ignition",
  "version": "0.0.0",
  "description": "",
  "keywords": [],
  "author": "Jeremy Green <hello@jeremy.green>",
  "license": "ISC",
  "main": "index.js",
  "scripts": {
    "build": "mkdir -p ../../tmp && zip -r ../../tmp/ignition.zip .",
    "update": "aws lambda update-function-code --function ignition --zip-file fileb://../../tmp/ignition.zip",
    "publish": "npm run lint && npm run build && npm run update",
    "lint": "npx eslint index.js",
    "test": "echo \"Error: run tests from root\" && exit 1"
  },
  "dependencies": {
    "config": "^3.3.1",
    "uuid": "^8.3.0"
  }
}
